export const RABBITMQ_SERVICE = 'RABBITMQ_SERVICE';

export enum Exchange {
  NEO4J_EXCHANGE = 'neo4j-exchange',
}

export enum RoutingKey {
  NEO4J_USER_PERSISTED = 'neo4j.user.persisted',
}

export enum Queue {
  NEO4J_QUEUE = 'neo4j-queue',
}
